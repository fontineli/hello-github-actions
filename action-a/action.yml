name: "Hello Actions"
description: "Greet someone"
author: "hd@goodquestion.dev"

inputs:
  MY_NAME:
    description: "Who to greet"
    required: true
    default: "World"

runs:
  using: "docker"
  image: "Dockerfile"

branding:
  icon: "mic"
  color: "purple"
